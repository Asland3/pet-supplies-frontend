/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "primary-production-60cc.up.railway.app",
      "pet-supplies-laravel-production.up.railway.app",
    ],
  },
};

export default nextConfig;
